# run this inside a proton folder (the one you're using for bf4)

sudo setcap cap_net_raw+epi dist/bin/wineserver

sudo setcap cap_net_raw+epi dist/bin/wine-preloader

sudo setcap cap_net_raw+epi dist/bin/wine64-preloader

